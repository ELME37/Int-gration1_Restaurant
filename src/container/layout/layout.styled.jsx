import styled from 'styled-components';

import { colors } from '../../utils/colors';

export const LayoutDefaultStyle = styled.div`
    width: 100%;
    margin: 0 auto;
    background-color:  ${colors.gray};
`;

export const LayoutFluidStyle = styled.div`
    width: 90%;
    margin: 0 auto;
    background-color: ${colors.gray};
`;